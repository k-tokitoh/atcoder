## vector

```cpp
#include<vector>
using namespace std;

// 初期化
vector<int> a;

// 要素数を指定して初期化
vector<int> a(10);

// 全要素共通の値を指定して初期化
vector<int> a(10, 777);

// 全要素共通の値を指定して初期化
vector<int> a{2,4,6};
```
